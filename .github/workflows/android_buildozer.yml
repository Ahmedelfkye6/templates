name: Android Buildozer

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    # تغيير إصدار Ubuntu إلى 22.04
    runs-on: ubuntu-22.04

    steps:
    - name: Checkout Cloud Code
      uses: actions/checkout@v5

    - name: Set up JDK 17
      uses: actions/setup-java@v5
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Set up Python
      uses: actions/setup-python@v6
      with:
        python-version: '3.11'

    - name: Install Dependencies
      run: |
        echo "Updating system and installing dependencies..."
        sudo apt-get update -y
        sudo apt-get upgrade -y
        # استخدام البدائل أو الحزم الأحدث
        sudo apt-get install -y --no-install-recommends build-essential libsqlite3-dev sqlite3 bzip2 libbz2-dev zlib1g-dev libssl-dev openssl libgdbm-dev libgdbm-compat-dev liblzma-dev libreadline-dev libffi-dev uuid-dev tar lld
        # تثبيت `libncurses6` كبديل للحزمة المفقودة
        sudo apt-get install -y libncurses6
        echo "Installing Python packages with pip..."
        pip install --upgrade pip
        pip install buildozer cython virtualenv kivy

    - name: Build Android APK
      run: |
        echo "Building APK with Buildozer..."
        buildozer android debug --no-interaction

    - name: Upload APK Artifact
      uses: actions/upload-artifact@v4
      with:
        name: VideoFX-Android-APK
        path: bin/*.apk
